version: 0.2
phases:
  build:
    commands:
      - echo "Build started on `date`"
artifacts:
  files:
    # This tells CodeBuild to take all files from the repository
    # and put them in the final .zip artifact.
    - '**/*'